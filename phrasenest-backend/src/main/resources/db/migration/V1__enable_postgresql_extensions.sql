-- Helps with case-insensitive and typo-tolerant expression searching.
CREATE EXTENSION IF NOT EXISTS pg_trgm;

-- pgvector will be enabled when semantic search is implemented.
-- CREATE EXTENSION IF NOT EXISTS vector;
